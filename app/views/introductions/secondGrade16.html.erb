<div class="container">
    <h3 class="text-center my-4">8기 - 16학번</h3>
    <div class="row">
        <% @secondGrade16.each do |student| %>
            <div class="col-lg-3 col-sm-4 col-6">
                <%= image_tag "/assets/secondGrade16/#{student}" ,class: "img-thumbnail"%>
                <%= content_tag(:p, "#{student.split('.').first.split(' ').last}", class: ["text-muted", "text-center"])%>  
            </div>
        <% end %>
    </div>
</div>