<header class="makePublic d-flex align-items-center">
  <div class="container">
    <div class="intro-text align-items-start">
      <div class="intro-heading">
        <a href="/alumnusfees">동창회비</a>
      </div>
    </div>
    <%# <div class="intro-text justify-content-end">
      <div class="intro-lead-in justify-content-end">
        <p class="ml-auto" style="text-align: right;">
          <a class="" href="/make_publics/meeting">회의록</a> |
          <a class="" href="/make_publics/account">회계자료</a> |
          <a class="" href="/make_publics/other">기타</a> |
          <a class="" href="/make_publics/rule">운영규정</a>
        </p>
      </div>
    </div> %>
  </div>
</header>
<div class="container my-5 table-responsive" style="min-height: 900px">
  <h3 class="text-center mb-4"></h3>
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">제목</th>
        <th scope="col">시간</th>
      </tr>
    </thead>
    <tbody>
      <% @alumnusfees.each do |f| %>
        <tr data-link="<%= alumnusfee_path(f) %>">
          <th scope="row"><%= f.id %></th>
          <td><%= f.title %></td>
          <td><%= distance_of_time_in_words(f.created_at,Time.now) %> 전</td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<script>
  $("tr[data-link]").click(function() {
    window.location = $(this).data("link")
  })
</script>